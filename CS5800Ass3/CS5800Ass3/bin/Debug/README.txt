The transition table file has the following format:

1st line shows total number of states, number of start states and number of finish states respectively

2nd line shows which states are the start states

3rd line shows which states are finish states

4th line to end of file shows state transitions in the format: "x y z" meaning from state x, if input is z go to state y